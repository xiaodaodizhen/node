// 第三方模块（我们下载的模块），不需要加./引得是 module.paths
//安装前npm init -y ，初始化。
// 初始化一个package.json 不能写注释，不能加标点
//如果没有初始化，记录依赖的功能就没了。

//开发依赖：webpack  babel 
//项目依赖：react redux


// npm install 包名字  --save-dev  安装开发依赖
// npm uninstall 包名字  --save-dev   卸载项目依赖

// npm 配置文件，可以更改下载的来晕啊，和下载到什么位置，代理配置； npm config list   npm config ls -l
 



//yarn
//全局命令，在命令行下使用，
// npm root -g 查看全局的包安装地址
// npm install -g yarn
// yarn init -y
// yarn add 文件名@版本号 --dev
// yarn remove 文件名
// yarn install 